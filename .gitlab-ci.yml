stages:
  - build
  - deploy


build:
  stage: build
  script:
    - make test

deploy:
  stage: deploy
  script:
    - make build deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "v2"
      when: always
