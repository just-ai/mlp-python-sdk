# coding: utf-8

"""
    OpenAPI definition

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: v0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
from inspect import getfullargspec
import json
import pprint
import re  # noqa: F401

from typing import Any, List, Optional
from pydantic import BaseModel, Field, StrictStr, ValidationError, validator
from mlp_api.models.multiply_modifier import MultiplyModifier
from mlp_api.models.override_modifier import OverrideModifier
from mlp_api.models.sum_modifier import SumModifier
from typing import Union, Any, List, TYPE_CHECKING
from pydantic import StrictStr, Field

MODELPRICINGMODIFIERSINNER_ONE_OF_SCHEMAS = ["MultiplyModifier", "OverrideModifier", "SumModifier"]

class ModelPricingModifiersInner(BaseModel):
    """
    ModelPricingModifiersInner
    """
    # data type: MultiplyModifier
    oneof_schema_1_validator: Optional[MultiplyModifier] = None
    # data type: OverrideModifier
    oneof_schema_2_validator: Optional[OverrideModifier] = None
    # data type: SumModifier
    oneof_schema_3_validator: Optional[SumModifier] = None
    if TYPE_CHECKING:
        actual_instance: Union[MultiplyModifier, OverrideModifier, SumModifier]
    else:
        actual_instance: Any
    one_of_schemas: List[str] = Field(MODELPRICINGMODIFIERSINNER_ONE_OF_SCHEMAS, const=True)

    class Config:
        validate_assignment = True

    discriminator_value_class_map = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = ModelPricingModifiersInner.construct()
        error_messages = []
        match = 0
        # validate data type: MultiplyModifier
        if not isinstance(v, MultiplyModifier):
            error_messages.append(f"Error! Input type `{type(v)}` is not `MultiplyModifier`")
        else:
            match += 1
        # validate data type: OverrideModifier
        if not isinstance(v, OverrideModifier):
            error_messages.append(f"Error! Input type `{type(v)}` is not `OverrideModifier`")
        else:
            match += 1
        # validate data type: SumModifier
        if not isinstance(v, SumModifier):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SumModifier`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in ModelPricingModifiersInner with oneOf schemas: MultiplyModifier, OverrideModifier, SumModifier. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in ModelPricingModifiersInner with oneOf schemas: MultiplyModifier, OverrideModifier, SumModifier. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: dict) -> ModelPricingModifiersInner:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> ModelPricingModifiersInner:
        """Returns the object represented by the json string"""
        instance = ModelPricingModifiersInner.construct()
        error_messages = []
        match = 0

        # deserialize data into MultiplyModifier
        try:
            instance.actual_instance = MultiplyModifier.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into OverrideModifier
        try:
            instance.actual_instance = OverrideModifier.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into SumModifier
        try:
            instance.actual_instance = SumModifier.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into ModelPricingModifiersInner with oneOf schemas: MultiplyModifier, OverrideModifier, SumModifier. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into ModelPricingModifiersInner with oneOf schemas: MultiplyModifier, OverrideModifier, SumModifier. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        to_json = getattr(self.actual_instance, "to_json", None)
        if callable(to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> dict:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        to_dict = getattr(self.actual_instance, "to_dict", None)
        if callable(to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.dict())


